#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char str[] = "do not approach catwoman i warn you";

    printf("%s\n", cap_string(str));

    return (0);
}
